apiVersion: v1
kind: Pod
metadata:
  name: argocd-pod
  namespace: default
  labels:
    type: myapp
spec:
  containers:
    - name: nginx-container
      image: nginx
      ports:
        - containerPort: 80
---
apiVersion: v1
kind: Pod
metadata:
  name: argocd-pod1
  namespace: default
  labels:
    type: myapp
spec:
  containers:
    - name: nginx-container
      image: nginx
      ports:
        - containerPort: 80
